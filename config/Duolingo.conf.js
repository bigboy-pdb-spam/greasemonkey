/*

Script requires getUsers function that returns object containing user email
 addresses and the names of the people and passwords that correspond with them
 to be declared as follows:

function getUsers() {
  return {
    USER_EMAIL: {
      name: NAME,
      password: PASSWORD
    },

    ...

    USER_EMAIL: {
      name: NAME,
      password: PASSWORD
    }
  };
}

*/
