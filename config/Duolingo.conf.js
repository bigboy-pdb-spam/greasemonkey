/*

Script requires users variable containing user email addresses and the names of
 the people and passwords that correspond with them to be declared as follows:

let users = {
  USER_EMAIL: {
    name: NAME,
    password: PASSWORD
  },

  ...

  USER_EMAIL: {
    name: NAME,
    password: PASSWORD
  }
}

*/
