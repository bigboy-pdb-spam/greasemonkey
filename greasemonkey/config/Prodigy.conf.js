/*

Script requires users variable containing user login names and the names of the
 people and passwords that correspond with them to be declared as follows:

let users = {
  USER_LOGIN_NAME: {
    name: NAME,
    password: PASSWORD
  },

  ...

  USER_LOGIN_NAME: {
    name: NAME,
    password: PASSWORD
  }
}

*/
