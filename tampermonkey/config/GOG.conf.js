/*

// Returns IDs of games that I'm not interested in
function getUninterested() {
  return [
    ID_NUMBER, // GAME NAME

    ...

    ID_NUMBER // GAME NAME
  ];
}

// Returns games that I might get
function getPerhaps() {
  return {
    ID_NUMBER: REASONABLE_PRICE, // GAME NAME

    ...

    ID_NUMBER: REASONABLE_PRICE // GAME NAME
  };
}

// Return styles for games based on their prices
function getPriceRanges() {
  return [
    // The default style
    {
      style: CSS_STYLE
    },

    // Style for price range
    {
      start: STARTING_PRICE_IN_WHOLE_DOLLARS,
      end: ENDING_PRICE_IN_WHOLE_DOLLARS,
      
      style: CSS_STYLE
    },

    ...

    // Style for price range
    {
      start: STARTING_PRICE_IN_WHOLE_DOLLARS,
      end: ENDING_PRICE_IN_WHOLE_DOLLARS,
      
      style: CSS_STYLE
    }
  ];
}

*/
